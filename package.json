{
  "name": "nutrikitchen-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:server": "cd server && pnpm dev",
    "dev:client": "cd client && pnpm dev",
    "build:server": "cd server && pnpm build",
    "build:client": "cd client && pnpm build"
  }
}
